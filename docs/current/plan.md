# 完整研究计划：从规则体系到可检验的持续行为

更新：2026-09-24。规划核对基线：`a3a083e2f9907ce0e29a20d2e133723fab13a207`。文档 PR #5 和业务 PR #4 已合并，`main` 是唯一现行基线。本页是唯一现行路线图；历史报告里的“下一步”只代表当时安排。

**本次更新只调整文档，不执行实验、不改变规则、不授权真实模型请求。下面新实验规模均为待审核建议，不是已注册协议或统计功效保证。** 已实现、历史报告记载、待实现和待验证严格分开。

导航：[当前工作台](README.md) · [三个会议问题](three_core_questions_experiment_summary.md) · [人工讨论单](../review/decisions.md) · [运行手册](runbook.md)

## 一、最终目标与当前最重要的判断

我们的目标不是无限增加对象、状态或规则，而是建立一个**规则可维护、事实可追溯、行为可执行、长期结果可评价、成本可控制**的虚拟人系统。

```text
对象目录：世界中有什么，对象参数从哪里来
人物状态：现在拥有什么、需要什么、做到哪里
共享规则：什么条件允许动作，动作具体改变多少
模型决策：在给定事实与候选中选择下一步
持续执行：推进时间、保存结果、生成下一次观察
评估与成本：哪里正常、哪里失败、每完成一件事花多少
```

当前不再重复证明 Q3 的单步目录优势或 Q5 的虚拟索引扩展性。最紧迫的问题是：**已保存、已展示的事实，怎样帮助模型减少不可执行选择；合法但可能奇怪的选择，怎样评价而不靠强制日程掩盖。**

合并代码不等于科学问题全部解决，也不等于可以启动长时间付费运行。

## 二、已经完成什么，哪些不能重做或扩大解释

| 范围 | 已有证据 | 当前保留结论 | 仍缺什么 |
|---|---|---|---|
| Q3 对象集 | 43 组共同成功案例，A/B/C 可执行数为 13/43、43/43、38/43 | Catalog + Top-K 是当前工程参考 | 开放新对象、真实参数质量、动态目录并未解决 |
| Q4 状态可见性 | 同一后台64字段真值，比较R8/R16/R32/R64；33组共同成功 | 最小集合未知；R16→R32这一组合低边际收益 | 相关字段投影及同长度语义消融 |
| Q5 规则扩展 | 固定类型/能力/模板，10M虚拟对象下索引不按对象复制 | 受测规则图结构可扩展 | 真实payload、例外规则、更新和并发负载 |
| Q6 持续状态 | 事务、幂等、人物—对象进度与7/30天脚本验收 | 受测工程一致性通过 | 真实模型自主长期行为 |
| Q6.1 Attempt 3 | 真实4次请求，3次完成，第4次PLAY未拥有游戏而被拒绝 | 状态变化及反馈有局部证据；原正式结论仍为INSUFFICIENT_EVIDENCE | 原4次全部接受门槛、媒体和所有权变化覆盖 |
| Q6.2 当前工程 | 只读活动投影、原产物核验、双世界A/B入口、默认零请求试运行 | 工程准备完成并已入main | 真实模型收益尚未测试 |

来源：[Q3](../studies/q3_object_set.md)、[Q4](../studies/q4_resource_visibility.md)、[Q5](../studies/q5_rule_scaling.md)、[Q6](../studies/q6_continuity.md)、[Q6.1](../studies/q61_real_pilot.md)、[Q6.2](../studies/q62_action_projection.md)。合并验收账本记录全库 **685 passed、8 skipped**；这是已有验收，不是本次规划重新跑出的数字。八项跳过需要未入Git的历史真人语料。[验收账本](acceptance.md)

不要再把以下事项作为新项目重复建设：寻找父仓库、重新合并PR #4、重建已经有效的Python3.12运行时、重跑q61-runtime-01、重新写已有投影和八请求A/B入口。环境损坏或代码确实变化时才做对应范围的复核。

## 三、阶段路线与依赖

下列 M 编号只是本计划的里程碑，不新增或重命名历史 Q 实验。

| 优先级 / 阶段 | 主要问题 | 进入条件 | 交付与退出标准 | 当前状态 |
|---|---|---|---|---|
| P0 / M0 | 新实验能否留下可信证据？ | 当前main | 小范围检查执行版本、计数、失败恢复及指标分母；缺什么补什么 | 待做增量检查，不重搭框架 |
| P0 / M1 | 可执行候选是否改善模型选择？ | M0通过、D-01批准对应协议/预算 | Q6.2真实对照报告；区分联调与固定状态证据，允许结果无优势 | 原短链入口就绪；固定状态面板待实现 |
| P0 / M2 | 缺对象时有没有合法获取路径？ | D-02确定范围；M1基线冻结 | 新版获取活动及成功/失败/恢复测试，不污染旧对照 | 待语义确认与实现 |
| P1 / M3 | Resource应保留什么、展示什么？ | D-04确认字段与评分；活动接口固定 | 同长度相关字段消融、成本—质量比较，不硬定16或64最优 | 待设计与实现 |
| P1 / M4 | 小模型能否使用这套规则与短上下文？ | 有冻结面板和模型接入条件 | 一个参考模型与一个本地候选的同协议结果 | 待条件确认；不必等多日实验后才开始 |
| P1 / M5 | 能否连续合理生活，而非只是不出错？ | M1/M2必要路径通过、D-05定义标准 | 独立场景覆盖→12决策→半天/一天；逐档批准和停止 | 部分工程已有，真实长期待验证 |
| P1→P2 / M6 | 大目录及共享规则如何长期维护？ | D-03/D-06限定目录、字段、变更负载 | 真实记录存储、参数更新、例外规则和候选检索分项压测 | 可先与M1并行做零模型工作 |

推荐顺序是 **M0→M1→M2→M3/M4→M5**。M6先做小规模离线维护验证，不抢占真实行为实验预算。采购语义、长期行为标准可以并行讨论，但在M1期间不能变更实验世界。

## 四、M0：下一轮先完成最小证据检查

这不是重新跑全部离线准备。只检查当前真实入口尚未充分说明的部分，并把差距写入一次交接记录。

1. **版本与环境。** 固定执行commit、fixture/config摘要、候选投影版本、模型请求别名、实际响应后端、Python及依赖；不写密钥。Q6.2现有CLI只接收三个`CONTINUITY_*`进程环境变量，不支持把Q6.1的`--env-file`参数直接照搬过来。
2. **请求与停止计数。** 区分已计划单元、应用调用尝试、有HTTP响应、有效提案、规则接受、活动完成；没有HTTP响应不能断言服务端收到。保留旧指标定义，新补充口径另列，不改历史记录。
3. **中断留痕。** 当前A/B在每臂结束时导出JSON/JSONL。确认请求开始/结束及已完成决策能从现有持久记录恢复；若存在空白，只补安全增量日志或只读导出。断开连接后先查原session，不换ID补跑，不把未知写为0。
4. **指标边界。** 对缺token、未触发媒体、未发生所有权变化、未知provider状态使用明确的缺失/覆盖标记。无事件时不变量为真，不等于已测试该机制。检查现有`q6_2_ab.py`聚合在异常、取消、无分母下的口径。

验收：零真实模型请求；必要的故障路径测试和文档审计通过；列清本轮修了什么、没改什么。若没有新增缺口，直接保留已有证据进入M1，不为获得新的测试数字而重复建设。正式代码变化从main开新分支，经相关专项与CI后冻结。

## 五、M1：先把Q6.2的真实收益测清楚

### 5.1 两种实验必须分开

| 方案 | 现在是否已有 | 用途 | 不能声称什么 |
|---|---|---|---|
| 现有自主短链A/B：两臂各最多4次，共最多8次 | 已实现、只做过零请求试运行 | 低预算联调，观察候选接线、状态反馈、停止行为和实际成本 | 后续状态会分叉，不能把第n步当同一状态的配对因果实验 |
| 新的固定状态配对面板 | 尚未实现，本计划建议 | 在相同状态下比较A_RAW与B_FEASIBLE，隔离提示干预 | 小型面板仍不代表总体、人类相似性或多日稳定性 |

**建议：现有8请求短链可作为一次可选联调，但不是必经步骤，也不是验收B更好的依据。主研究采用固定状态面板。** 若重点是尽快得到可解释比较，可以不消耗联调预算，先实现面板再申请正式请求。两个方案的授权、session、数据和结论必须分别记录。

### 5.2 固定状态面板建议

先用 **12个冻结状态×2条件×2次重复＝48次请求** 做有界探索；每个状态—重复单元是一个配对。若另做一次8请求联调，合计上限建议为56次；不含任何额外网络探针。两个预算都需另获明确授权，不能将本计划当成授权。

面板预先覆盖六类条件，每类两个对照状态：游戏未拥有/已拥有；餐前/餐后但仍饥饿；媒体进行中/已完成；活动地点符合/不符合；余额与库存允许/不允许获取餐食；休息与工作等前置条件满足/不满足。具体状态必须由当前规则确认，避免同时制造多个极端冲突；必须保留正常、可执行状态，不能只挑D4式失败来保证B获胜。

每个A/B配对从同一冻结快照复制出独立world，只提出并执行一个高层活动；执行结果不能流入下一配对。配对内AB/BA交替并用固定seed安排顺序；保存初态hash、候选hash及分配表。重复两次只能用于小规模探索，不是两个独立人群。

A保持原观察与提示；B只增加当前可执行活动/对象对。对象、规则、状态、先验、provider契约和输出限制固定；不同时加入近期摘要、采购新行为、禁止重复吃饭或关闭思考的新参数。场景答案、未来状态、评分标签不得进入提示或投影器。

### 5.3 评分必须独立于候选生成

主要比较以**实际RuleEngine在独立副本上的执行结果**为准，而不是“模型选了我们给出的候选，所以一定正确”。同时报告投影成员资格、启动接受、活动完成、commitment失败、状态不变量、上下文和成本。

统计至少并列呈现：计划数、已尝试数、有效提案数、拒绝数/有效提案数、接受数/有效提案数、完成数/有效提案数、完成数/计划数；完整配对分析与所有已分配单元的覆盖情况都保留。超时、未运行、无效输出不并入行为性拒绝；NA不补0。

同状态面板另列“A拒绝/B接受”“A接受/B拒绝”等不一致配对，以及按场景家族的分布。重复观测按场景汇总，不把同场景重复当完全独立样本。48次仅是探索预算；是否需要更大样本，在看清不确定性后另立协议，不边看结果边无限加样本。

### 5.4 预注册停止条件与结果分流

现有短链协议不修改：规则拒绝/commitment失败停止本臂；provider、输出或架构错误停止整个session。新面板建议：规则拒绝是该单元的有效观测，记录后进入下一个预定独立单元；provider/输出/架构、状态不变量或证据完整性故障停止整轮。具体故障枚举必须在D-01批准并在代码测试中冻结，不能在运行中临时改变。

所有模式禁止retry、JSON修复、模型/endpoint切换、修改状态后补跑和脚本替代。出现未知发送状态先核对原记录，不重新发送。

结果允许四种结论：**有观察到的改善、无清楚差异、观察到退步、证据不足**。若B减少拒绝但代价过高，尝试紧凑表示属于下一份协议；若A/B都好，不能声称证明B必要；若B变差，保留A并查候选覆盖/表达；若规则或投影错误，先修工程，不调角色偏好遮掩。

交付：冻结分配表、逐单元安全指标、中文比较报告、失败案例、成本表、局限及下一步决策。验收通过表示证据完整、可解释，不要求B必须获胜。

## 六、M2：补齐“想玩游戏但没有游戏”的合法路径

这对应会议对象集问题的下一层：**目录里有对象，不等于人物有能力获取或使用它。**

待D-02确认：当前世界仅允许已有物品；还是增加显式获取宏活动。建议在新实验版本里优先做一个最小获取活动，暂称`ACQUIRE`，具体命名和数据结构待审。不要在冻结Q6.2里偷偷让PLAY自动购买，也不要免费赋予所有权。

获取链应明确：目标/卖家→旅行→余额及库存检查→交易→所有权→完成。规则模板定义过程；对象记录提供不同价格、效果和时长。旅行途中发生费用/时间消耗后再失败，要保留已发生事实，不能假装整个宏活动从未开始；单笔交易的账本和库存则需原子一致。

最小测试包括：已拥有、库存为0、钱不足、旅行后买不到、途中下架、重复命令、断点恢复、并发争用最后一件、别名同一ID、购买后PLAY可达。不采用“购买成功”作为所有测试唯一结局。

退出标准：需要购买的任务能走合法路径；失败分类与账本正确；预览/执行条件一致；不修改旧Q6.1/Q6.2冻结结果。先零模型工程验收，再在新版本申请模型实验。

## 七、M3：真正推进Resource问题，不再只扩大字段数

[Q4](../studies/q4_resource_visibility.md)测试的是可见字段，不是后台真值维度。下一步先产出一张**字段合同表**：含义、单位/方向、来源、初始化、更新公式、缺失值、影响哪些活动、属于持久事实还是展示摘要。

候选输入比较建议为：固定紧凑字段；在相同字段/近似token预算下的相关字段投影；完整参考字段。继续复用同一底层真值与现有场景，不因某臂看不到就从世界删状态。先复用Q4固定面板做独立研究，不能直接把其64字段硬塞进Q6状态库。

相关字段选择器只能依据当前可观察状态、已声明规则依赖和候选活动，不读答案标签或事后选择；不能因为投影只保留某行为，就再隐藏其他需求，形成自我强化。关键预算、位置、所有权、活动冲突等执行事实保持可核对。R1先验保持一致。

主要评估是任务适当性、可执行性、关键字段可见性和成本。对齐率与关键遗漏率若互为补数，不能当两份独立证据；留出日记指标保留NA分母，不能当“真人正确率”。

重复次数、质量容忍差和上下文目标由D-04预先确定。本计划不宣布最优16维或64维。只有相关投影在同预算比较中提供有用证据，才进入Q6新版持续世界；未通过就保留当前紧凑输入。

## 八、M4：尽早做一个本地小模型对照

模型替换不必等到30天实验。M1形成稳定面板、输出合同和成本口径后，即可核对硬件与接入条件，选择**一个**本地候选与既有远端参考对照，避免一次铺开多个模型。

记录模型标识/权重版本、量化、推理服务、上下文和输出预算、并发、温度及思考设置。相同世界、场景、合法性规则和候选投影不变；先分开报告模型差异与输入裁剪差异，不同时调多处追求成功。请求别名不是实际后端身份，未观察到的身份不补写。

按请求和每完成活动分别记录token、耗时及资源成本；本地成本不能直接视为0，价格依据未核对时只报实测资源和token、不编造金额。先单Agent短请求，达到预先批准的质量/成本条件后再扩并发。

输出：可用性、规则拒绝、状态利用、截断/超时、成本差异与失败样例。小模型结果不佳不立即削弱规则，也不能拿远端参考成绩替代本地证据。具体模型、硬件、预算和容忍差均待确认。

## 九、M5：长期行为分三层验收

### 9.1 先明确什么叫“正常”

| 层次 | 看什么 | 判定方式 |
|---|---|---|
| 硬事实一致性 | 钱/库存/所有权、时间、幂等、状态版本 | 确定性检查；任何真实违反必须调查 |
| 任务与进度连续性 | 看至第几集第几分钟、暂停/恢复、获取后使用 | 带覆盖标记的场景测试，不靠未触发时的true |
| 行为适当性 | 需求响应、义务履行、异常循环、偏好和分布 | D-05预先制定场景标准，存在分歧时人工复核 |

连续吃两次、显式重看一集或主动休闲不自动算错。异常循环应结合状态未改善、同一失败反复出现、没有新信息及持续窗口判断。不能靠强制几点工作/吃饭、硬禁重复或fallback WAIT制造好看轨迹。

### 9.2 覆盖先于时长

在独立新fixture中覆盖媒体暂停后续播、主线下一集与显式重看、所有权获取后使用、餐后仍有需求、物品下架与失败恢复。允许用脚本测试这些工程场景，但明确标注SCRIPTED；真实自主链中未选到WATCH，不能插WATCH后声称模型覆盖了追剧。

已有7/30天工程脚本保留，不重复当新模型研究。新版规则变化只做受影响回归及必要全库验收。

### 9.3 逐档扩展

建议顺序：短链→12次高层决策→半天/一天→多天；每档分别规定**模拟时间上限与模型请求上限**，不能把12次请求等价为一天。每档新授权，不继承无限运行许可。

分别记录计划/尝试/接受/完成、活动占用时间、即时动作链、provider等待、行为性空闲、预算截断和任务覆盖。游戏采购/正常行为标准未定前，不做声称开放自主生活的长跑。

进入下一档需要：没有未解释的状态或预算错误；故障可定位；目标任务覆盖达到预先要求；成本可接受。四次全部接受也不证明真人行为，多日脚本也不证明多日自主模型行为。

## 十、M6：把规则维护从虚拟索引推进到真实记录与变更负载

这是规则负责人可并行做的零模型工作。[Q5](../studies/q5_rule_scaling.md)已经说明不必给每个对象复制规则边，后续不用再重复同一个10M算术ID实验。

先确定最小合同：Object记录包含稳定ID/别名、type/capabilities、带单位与来源的属性、版本、可用性；Rule Template包含适用范围、前置条件、效果函数、失败码与版本。人物拥有量/观看进度是稀疏人物—对象状态，不塞回共享目录。规则写“如何计算”，对象参数写“变化多少”。

建议由小规模实际存储记录开始，例如1K→10K→100K；这里必须有payload、索引、读写和变更，不只虚拟ID。可以用声明为合成的参数化记录，不能冒充真实食品/游戏测量。只有业务需要并满足资源预算后再扩1M/10M，不预设必须建全世界目录。

必测负载：参数变化、模板更新、能力变化、例外条目、上架/下架、别名合并、版本迁移与并发库存；小目录先与朴素穷举结果对照。更新后哪些对象受影响应可解释，不默认编译全量Object×Rule边。

分别报告规则索引、对象payload、检索索引、人物—对象稀疏状态及进程总内存；延迟包括p50/p95、冷/热状态与更新成本。固定schema的常数索引不能推广为类型/例外数增长时仍常数。

有限运行目录、每次Top-K和动态上下架是三种不同控制量。先固定目录复现，再独立测试上下架策略；同时保留可用性与曝光次数，不能把低曝光误读为低偏好。世界事件必须版本化并留痕，不用临时“公司倒闭”解释随机删对象。

## 十一、下一轮可直接交接的任务包

| 编号 | 下一项具体动作 | 完成物 | 本次是否已执行 |
|---|---|---|---|
| T-01 | 按M0检查Q6.2执行版本、逐请求留痕和异常指标，差距最小化修复 | 一页差距表、必要测试；0真实请求 | 否，仅已列计划 |
| T-02 | 在D-01选择“只做8请求联调”或“固定状态主研究”，是否跳过联调 | 有范围、预算、顺序和停止条件的明确决定 | 否，待人工 |
| T-03 | 选择主研究时实现12状态配对面板，不重写旧双短链runner | 新的独立协议、冻结清单、离线验证 | 否，待批准实施 |
| T-04 | D-02确定采购范围，D-05列长期行为正反例与待判例 | 两份语义决定，先不混入M1 | 否，可先讨论 |
| T-05 | D-04准备Resource字段合同；D-03/D-06准备真实目录合同 | 字段—规则—效果映射与小规模维护负载 | 否，可并行零模型整理 |

建议审核职责：规则负责人审核对象/规则/获取语义；研究负责人审核对照与评分；工程执行者实现和留痕。未指定具体成员，也不把建议角色视为已接任务。

**最快得到下一份有用结果的路径：T-01完成增量检查→T-02明确协议→执行已授权的小实验并解释差异。不要先做新UI、全目录搬迁、全量重新校准或多人社会长跑。**

## 十二、每轮结束怎样决定继续、暂停或保留基线

工程通过与实验收益分开：工程必须可运行、可停止、可追溯；研究允许零差异和负结果。失败先归类为环境/传输/输出/选择/规则/commitment/状态反馈/评价口径，不能一概称为“发呆”。

所有新阶段必须写：问题、唯一干预、基线commit、场景来源、预算与停止规则、全部单元状态、分母、原始产物位置、安全摘要、局限、决定。没有试验不能填结果；尚未触发不能填已验证；预算耗尽不是自然活动完成。

断线或执行状态未知先只读查询，不擅自追加请求。安全退出保留已有日志；只读恢复导出不算补跑。修复后如需新实验，要有新冻结版本和新授权，旧失败保留。

## 十三、代码、文档与数据维护约定

后续从最新main开短期`research/`或`fix/`分支，不继续向已合并PR #4/#5追加新任务，不force push。轻量`.venv-q61-runtime`用于模型入口；完整AS2测试环境独立，不为跑全库把全部依赖塞回轻量环境。官方子模块仍固定`670c94fff7c64c4f79b632125f2ccf968155e746`，升级另立决定。

不再新建“最终计划v2/v3”。路线只改本页；[人工决定](../review/decisions.md)记录批准；[问题档案](research_status.md)记录证据；[验收账本](acceptance.md)记录实际测试版本与结果；[汇报专区](../reports/README.md)提炼结论。正常阅读链接指向中文文档，代码名称和机器标签保留。

不覆盖q61-runtime-01、原训练/评估划分、旧失败或其他成员delivery数据。后续目标意图集/交付数据的兼容性单独验收，不把未审阅数据默认映射成新行为。原报告继续通过[证据登记](../reference/evidence.md)固定Git历史追溯。

本次仅完成计划和关联说明更新；真实Q6.2 A/B仍为`NOT RUN`，Q4最小集合仍为`UNRESOLVED`，新采购活动、固定状态runner、相关Resource投影和真实目录维护均不是本次已经实现的功能。
