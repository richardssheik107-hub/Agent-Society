# Q3/Q4/Q5 翻译前原文

此目录保存三份报告翻译前的准确字节，均复用原 Git blob，没有重新撰写。

main 中原有报告路径已按用户要求改为中文阅读版；原文完整性校验通过 `cleanup_manifest.json` 的显式映射指向此目录。校验仍与冻结 Git 提交比对 SHA256，不能用更新哈希或跳过校验掩盖历史变更。

这是翻译与原文并存，不是撤回中文翻译，也不是改写原实验数据。
