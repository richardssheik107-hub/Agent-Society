# 人工讨论单与决策记录

更新：2026-09-24。D-01—D-07仍待审核，D-08已完成。Q6.1/Q6.2已入main不代表真实A/B获准。路线和具体任务只维护在[完整计划](../current/plan.md)，本页只维护选择与授权；以下新增建议均不是已批准事实。

<a id="d-01"></a>
## D-01｜Q6.2比较对象、预算与停止规则（P0）

**已有：** 原始Attempt3逐字段核验通过；只读投影、双独立world、AB/BA顺序、每臂最多4次/总8次和默认零请求入口已实现。真实对照未运行。

**需选择的两个用途：** 现有8请求双短链用于低预算联调，后续状态分叉，不逐步当同态配对；新的固定状态面板用于同一状态的提示对照，尚未实现。建议主研究用固定状态面板，8请求联调可选，不强制都跑。

建议面板规模为12状态×2条件×2重复=48请求；若另做一次8请求联调，合计建议上限56，不包含额外探针。预算是探索规模，不保证统计精度，尚未授权。详细场景、分母、配对与分流见[计划M1](../current/plan.md#五m1先把q62的真实收益测清楚)。

批准前固定：是否联调；是否批准新面板实施；场景清单、seed、顺序、唯一干预、主要评分、请求上限、异常枚举、继续/停止规则、成本与缺失值。旧短链停止规则不变；新面板中规则拒绝后继续下一独立单元，是建议的新协议，不能偷换旧协议。

真实执行前完成[M0](../current/plan.md#四m0下一轮先完成最小证据检查)增量证据检查。只追加候选，不同时改采购、近期摘要、初态或思考设置。审核角色建议：研究负责人。**决定：待审核。**

<a id="d-02"></a>
## D-02｜对象获取与购买游戏活动（P0）

当前高层JSON不含BUY，PLAY不代购。过滤非法PLAY不等于产生合法采购路径。选项：限定已拥有目录；显式获取宏活动；更一般的受约束获取计划。

建议在新版本实现最小显式获取活动，暂称ACQUIRE；名称、范围和参数仍待定。先规定卖家/地点、余额、库存、旅行时间、失败、幂等、恢复与已拥有时的处理；旅行已发生后采购失败不能伪造时间回滚。获取后PLAY可达必须用测试验证。

不修改冻结Q6.2世界免费发游戏，也不把隐式购买塞进PLAY。[计划M2](../current/plan.md#六m2补齐想玩游戏但没有游戏的合法路径)。审核角色建议：规则负责人、研究负责人。**决定：待审核。**

<a id="d-03"></a>
## D-03｜对象目录范围、候选与动态上下架（P1）

Q3只支持有限目录单步接口优势，未完成动态目录实验。需分别决定后台总目录、一次模拟可用目录、一次提示Top-K三个范围，不把它们当同一个数量。

明确真实名称/合成占位、参数来源/单位/版本、NEW是否允许、对象新增审核和人物—对象持久状态。建议固定目录先可复现，再独立测试上下架；低曝光不等于不喜欢，下架应有可追踪世界事件。不能先追求千万真实对象规模。[计划M6](../current/plan.md#十m6把规则维护从虚拟索引推进到真实记录与变更负载)。审核角色建议：规则/数据负责人。**决定：待审核。**

<a id="d-04"></a>
## D-04｜Resource字段合同与同预算消融（P1）

最小集合仍未知，字段数与字段内容不能混淆。需确认含义、单位/方向、来源、更新公式、缺失值、规则依赖、持久事实/展示摘要的区分，以及质量容忍差、token目标和样本预算。

建议比较固定紧凑字段、同预算相关字段与完整参考字段，先复用Q4固定场景，不直接把64字段注入Q6。投影器不读评分标签或事后动作；对齐与遗漏若互为补数，不视为两项独立证据；日记NA分母单列。[计划M3](../current/plan.md#七m3真正推进resource问题不再只扩大字段数)。审核角色建议：研究负责人。**决定：待审核。**

<a id="d-05"></a>
## D-05｜正常长期行为、覆盖和扩展门槛（P0）

分开硬事实一致、任务进度连续、行为适当性三层。需明确重看/跳集、餐后仍有饥饿、重复购买、工作/睡眠义务、异常循环窗口、目标人群和人工分歧处理。

第一餐后245/1000的饥饿已由原产物核验，不能只因第二餐重复就判错。未触发WATCH或所有权变化不能视为已测试该能力。先定义带正反例的场景覆盖，再逐档批准12次决策、半天/一天、多天；模拟时长与请求预算分别定。

模型对照还需确定本地硬件、一个候选模型、推理版本和质量/成本容忍差；可在稳定短面板后开始，不必等多日长跑。任何吞吐扩展都不继承无限调用许可。[长期验收](../current/plan.md#九m5长期行为分三层验收)、[小模型对照](../current/plan.md#八m4尽早做一个本地小模型对照)。审核角色建议：研究/规则负责人。**决定：待审核。**

<a id="d-06"></a>
## D-06｜共享规则维护与真实目录里程碑（P1）

Q5固定合成schema/虚拟ID索引通过，不等于生产数据库完成。需确认对象/规则合同、例外规则、版本迁移、动态更新和并发负载，分别报告索引、payload、人物—对象状态和进程总内存。

建议小规模实际存储记录开始，先1K→10K→100K；只在需求和资源条件明确后扩更大规模。检查参数变更、模板更新、下架、别名、幂等、库存竞争；小目录与朴素实现对照。不能只报17.9kB，也不能重复算术ID压测冒充真实搜索。[计划M6](../current/plan.md#十m6把规则维护从虚拟索引推进到真实记录与变更负载)。审核角色建议：工程/规则负责人。**决定：待审核。**

<a id="d-07"></a>
## D-07｜行为数据人群、年代、评分与使用边界（P1）

历史美国日记用于本体和时长结构，并不自动代表当代目标地区的游戏/视频生活。继续作为软先验，不替代合法性规则，不把群体频率当个人正确答案。

需确认目标人群、数据更新、加权、OTHER和NA解释、迁移验证及分享范围。保留原TRAIN/EVAL划分，不用评估集选投影字段；不把可获取等同任意再分发，本次不作新的法律判断、不上传原始数据。新增交付/意图数据须先审映射合同。[行为数据来源](../stages/02_behavior_data.md)。审核角色建议：数据/研究负责人。**决定：待审核。**

<a id="d-08"></a>
## D-08｜业务PR合并与验收口径（已完成）

**决定：已完成。** 2026-09-24，PR #4已合入main，merge为`cce3e7bd850f72faf0efa41017491d392b00ee11`。

先以main中文文档体系合入Q6.1/Q6.2代码、测试和CI，没有恢复旧archive、跳转和重复计划。第一次集成CI暴露旧archive_original_overrides测试不兼容；修复`bea7dbf64fd8a61eced09820503b46ae3f56ad59`后，按固定Git历史核验原文。

已有合并验收记录：全库685 passed、8 skipped；AS2、只读投影回放、A/B零请求试运行等通过。具体运行版本与范围见[验收账本](../current/acceptance.md)，不把本次文档更新说成重新运行了全部测试。

未授权范围仍包括真实Q6.2 A/B、12-call和多日付费运行。[分支状态](../current/branches.md)

## 每项决定填写模板

```text
议题编号：
决定：待审核 / 同意 / 否决 / 延后
审核人或角色：
日期：
依据的提交与协议版本：
批准范围（代码实施、场景、顺序、数据）：
真实请求授权：否 / 是（预算、session、停止条件）：
未批准范围：
执行负责人：
复核结果及证据：
```

没有明确决定不能视为执行授权；批准编写计划或代码不等于批准真实请求。回到[审核总表](README.md)。
