# Q6.2｜把当前可执行活动提前告诉模型

状态：研究分支已有工程实现和离线验证；真实 A/B 尚未运行，最新已核对代码没有真实 A/B CLI。不能把交给执行者的后续提示词当作已完成代码。[证据 E-Q62](../reference/evidence.md#q62)

## 研究问题

对象在目录里，不等于该人物现在能使用。Q6.1 的 `game_a.qty=0` 仍被选作 PLAY，提示应区分“存在的对象”与“现在可执行的活动/对象对”。

A_RAW 保持原 prompt；B_FEASIBLE 保留相同观察和目的地，追加 `executable_options` 及说明。干预只有当前合法候选，不添加最佳答案、偏好排序、近期变化摘要或日记标签。

## 工程做法

`ContinuityWorld.preview_activity()` 复用实际活动启动和购买检查；`StateStore.read_snapshot()` 用一致只读快照保护。预览不扣钱、不写事件、不占请求预算，也不通过“先执行再回滚”生成候选。

区分 `start_allowed` 与 `executable_now`：吃饭可能先允许出发，但按当前已知钱/库存无法完成购买。候选不保证未来库存不变，真正提交时仍由规则再校验。

最多使用旧观察中的五个对象，当前小世界四个；按 ID 的有限候选不是语义检索，也不是千万真实目录能力。未拥有游戏时不提供 PLAY，但不会免费发游戏、偷偷代购或篡改提案。

## 已有离线结果

默认按报告四步意图在新世界回放，模式 `SCRIPTED_REPORT_SEQUENCE_REPLAY`。本次已核对的 GitHub 记录中 `SOURCE_ARTIFACT_VERIFIED=False`，不是用户本地原 artifact 已验证。

| 步骤 | 冻结报告意图 | 回放饥饿值 | B 候选是否保留 | 原规则结果 |
|---|---|---:|---|---|
| 1 | TRAVEL restaurant | 800→815 | 是 | 完成 |
| 2 | MEAL food_meal | 815→245 | 是 | 完成 |
| 3 | MEAL food_meal | 245→0 | 是 | 完成 |
| 4 | PLAY game_a | 0→0 | 否 | ITEM_NOT_OWNED |

A/B 提示字符数依次 1236/1772、1243/1659、1245/1661、1245/1661。额外候选会增加成本，**没有 B 模型输出，不能报告真实拒绝率下降**。

已记录验收：Q6.2 新增 47 项；本地联合专项 160；目标 Python 3.12 CI 核心 119；全库 660 通过、8 项历史语料缺失跳过；固定 AS2、Ruff、32 份历史原文审计及 7/30 天脚本通过。不同范围不能相加为独立样本数。

## 目前尚需人来确定

第一，确认原 `q61-runtime-01` 文件与回放一致；第二，决定真实 A/B 用固定状态配对还是两条自主短链，二者回答的问题不同；第三，冻结是否显示阻塞原因/近期事件，这些都是额外干预；第四，采购宏活动另立议题。

若用两条自主短链，第一次选择后世界会分叉，不能继续声称每步状态相同；一次最多八请求只能当小试验，不能得稳定因果结论。投影排除非法候选也不保证模型遵循，更不解决真人合理性。[人工 D-01、D-02](../review/decisions.md#d-01)

代码所在分支：`research/q6-real-continuity-pilot`；模块 `continuity/action_projection.py`、`continuity/q6_2.py`；无模型核验脚本 `scripts/run_q6_2_projection_offline.py`。本轮整理不把这些业务代码合入 main，运行条件见[手册](../current/runbook.md)。
